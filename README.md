# 📊 Sales Report Automation

Python-скрипт для автоматизации обработки Excel-файлов с данными о продажах. Скрипт объединяет файлы, рассчитывает итоговые суммы и строит наглядный график динамики продаж для каждого файла.

🛠️ Функции скрипта:

- Объединение всех Excel-файлов из папки `data/` в один итоговый отчёт `Объединённый_отчет.xlsx`.
- Подсчёт итоговых сумм по всем файлам и сохранение в `Итоги_отчета.xlsx`.
- Построение графика `График_отчета_понятный.png`:
  - Каждая линия = отдельный Excel-файл.
  - Разные цвета для каждой линии.
  - Легенда с именами файлов.
- Автоматическая проверка наличия колонок `Дата` и `Продажи`.

📁 Структура проекта:
project/
│
├─ data/ # Excel-файлы с данными о продажах
├─ script.py # Скрипт для объединения и визуализации
├─ Объединённый_отчет.xlsx
├─ Итоги_отчета.xlsx
└─ График_отчета_понятный.png

